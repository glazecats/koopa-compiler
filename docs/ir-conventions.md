- Canonical IR is non-SSA, block-based, and verifier-backed; pass outputs must preserve entry reachability plus dense block/temp IDs.
- First-version lower IR is a downstream stage, not an in-place rewrite of canonical IR; phase 1 keeps the same function/basic-block/terminator CFG shape and only changes the value/storage boundary.
- First-version lower IR splits value refs from slot refs on purpose: value instructions consume temps/immediates only, while locals/globals appear only through explicit `load_*` / `store_*` slot operations.
- Lower IR must be able to accept any verifier-legal canonical IR, not only the output of the canonical default pass pipeline; canonical `ir_pass` cleanup may improve shapes, but it is not part of the lower-IR semantic contract.
- Current serial lowering preserves canonical CFG topology and canonical temp ids; any extra temps introduced to materialize slot reads are allocated above the canonical temp range.
- Current serial lowering rewrites canonical `mov` into either lower-IR `mov` or `store_*` depending on the destination kind, and rewrites canonical slot-valued `ret` / `br` conditions / `call` args / binary operands through explicit `load_*` instructions in the same source block.
- Lower-IR globals preserve canonical constant/runtime initializer flags as metadata, and the lower-IR verifier now mirrors the canonical runtime-init helper contract: reserved helper names are gated on runtime globals, `__global.init` / `__program.init` must keep the fixed zero-parameter zero-local single-block `ret 0` shape, and startup calls to `__global.init` are only legal from `main` entry or `__program.init` entry.
- Lower-IR verifier now also mirrors canonical top-level/public-call contracts: calls must resolve to known function entries with matching parameter counts, duplicate function/global names are rejected, and function/global name collisions are rejected.
- Lower-IR verifier must also be crash-proof on malformed metadata, not just semantically strict on well-formed tables: nonzero global/function/local/block counts with NULL backing tables are rejected explicitly rather than being allowed to reach raw table dereferences.
- Lower-IR verifier also treats count/capacity mismatches as structural contract failures, not “best effort” metadata: `global_count`, `function_count`, `local_count`, `block_count`, and `instruction_count` must all stay within their recorded capacities before any table walk begins.
- Lower-IR verifier now also treats CFG reachability as part of the public contract: body blocks must be reachable from `bb.0`, not merely locally well-formed and target-valid.
- Lower-IR builder APIs are no longer intentionally permissive on a few common malformed shapes: declaration-only functions fail fast on block append and temp allocation, parameter locals must remain a contiguous prefix, blocks reject instruction append after a terminator, and terminators are no longer silently overwritten.
- `lower_ir_lower_from_ir(...)` now self-verifies its own output before returning success, so lowering regressions fail at the production boundary rather than relying on every caller to remember an extra verifier pass.
- The current lower-IR phase-2 slice should now be treated as checkpoint-complete: representative lowering coverage, verifier hardening, and builder/API fail-fast are all in place, so further expansion should be need-driven rather than open-ended.
- The next likely structural step is value-SSA on top of lower IR, not more speculative hardening inside lower IR itself; use lower IR as the stable input layer and keep SSA scoped to value flow rather than memory SSA or machine/register concerns.
- The first value-SSA slice is now a real separate skeleton (`include/value_ssa.h`, `src/value_ssa/`, `tests/value_ssa/`), not only a design note: it has manual-construction builder APIs, a verifier, a dump surface, and isolated regression coverage.
- Value-SSA now also has a first real conversion entrypoint from lower IR; the current boundary is intentionally narrow and should be treated as such: straight-line CFGs, diamonds, simple slot-carried loops, and a representative loop-carried temp join are in scope, but broader cyclic CFG families are still deferred beyond this first loop-header phi slice.
- Value-SSA also now has a small, separate trivial-values simplify pass; keep its contract narrow on purpose: it may rewrite use sites through trivial `mov` chains and same-value `phi` nodes, but it is not yet a full dead-definition cleanup or SSA-renumbering pass.
- Value-SSA also now has a separate narrow CFG simplify pass layered after construction/conversion: it may fold same-target branches to `jmp`, fold immediate-condition branches to the taken successor, and then compact newly unreachable blocks plus surviving SSA value ids so the current verifier contract stays dense and reachable.
- Value-SSA also now has a separate narrow dead-def cleanup pass layered after simplify/construction: it may remove unused pure value definitions (`phi`, `mov`, `binary`, `load_*`), but it must continue to preserve effectful instructions such as `call` and `store_*`; after removal it should compact surviving SSA value ids so verifier legality stays dense.
- Value-SSA now also has a first shared CFG analysis/helper layer for later rename/construction work: predecessor counts, reachability, dominator matrix, immediate dominators, explicit dominator-tree children, dominance frontiers, iterated phi-placement closure, and dominator-tree preorder can be computed directly from a verifier-legal SSA function, with current regression authority locked on representative diamond and loop-header-phi shapes.
- Value-SSA rename groundwork now also includes a separate dominator-tree walk scaffold with explicit enter/leave callbacks. Keep future rename state on top of this split helper rather than re-embedding tree traversal into each rename experiment.
- Value-SSA rename groundwork now also includes a scoped rename-state stack: bindings can be pushed on block entry, shadowed in dominated children, and automatically unwound on block leave. Future rename work should build on this scope stack instead of hand-rolling ad hoc binding rollback.
- Value-SSA rename groundwork now also has an explicit use-site rewrite helper: given the current rename-state bindings, block-local uses in instructions and terminators can be rewritten without touching defs or phi inputs. Treat this as the narrow bridge between future scope stacks and future full rename logic.
- Value-SSA rename groundwork now also has an explicit phi-input edge rewrite helper: given the current rename-state bindings for one predecessor path, successor-block phi inputs for that predecessor can be rewritten without entangling edge updates with block-local use rewriting.
- Value-SSA now also has a first real rename orchestration over existing SSA functions: dominator-tree walk, scoped bindings, local use rewrite, local def renaming, and successor-edge phi-input rewrite are now composed into an alpha-renaming pass that preserves verifier legality while making scrambled SSA ids canonical in dominator order.
- Value-SSA now also has a first program-level canonicalization entrypoint: trivial value cleanup, trivial CFG cleanup, dead pure-def cleanup, and alpha-renaming can now run as one small pipeline to stabilize representative SSA dumps without needing each caller to stage those passes by hand.
- Current value-SSA dump naming should keep source-near slots readable as `a.0` / `g.0` while SSA values stay in a clearly separate internal namespace such as `ssa.0`, `ssa.1`, ...
- Current value-SSA conversion should live in its own source split (`value_ssa_from_lower_ir.inc`) rather than bloating `value_ssa.c`; keep growing conversion there first before considering deeper file reshaping.
- Current value-SSA bridge authority now includes a one-shot `value_ssa_build_canonicalized_from_lower_ir(...)` entrypoint: callers that want stabilized SSA should prefer the bridge over manually composing raw conversion plus canonicalization in ad hoc test/consumer code.
- The one-shot value-SSA bridge should now be treated as a stabilized-output boundary, not just a convenience wrapper around one cleanup pass: it performs an extra canonicalization convergence step internally, and representative bridge-authority shapes should remain unchanged if canonicalized once more by the caller.
- Current value-SSA canonicalization authority includes a fixed-point expectation on representative shapes: once a program has been canonicalized, a second `value_ssa_canonicalize_program(...)` run should not further change the dump.
- `value_ssa_canonicalize_program(...)` should now be treated as a self-verifying boundary, not only a convenience cleanup helper: it must reject malformed input SSA and guarantee verifier-legal output on success.
- Value-SSA now also has a shared internal side-effect/removable-def classification boundary: dead pure value defs may be eliminated, but effectful instructions such as dead-result `call` and all `store_*` must survive both isolated DCE and the full canonicalization pipeline.
- The one-shot lower-IR bridge inherits the same preservation contract: `value_ssa_build_canonicalized_from_lower_ir(...)` must not optimize away dead-result `call` instructions while stabilizing the converted SSA result.
- Value-SSA dead-def cleanup must also stay conservative for dangerous binary ops under the current safety policy: dead-result `div`/`mod`/shift instructions are non-removable because they can still embody runtime validation or trap behavior even when their SSA result is unused.
- The recent value-SSA result-layer hardening slice should now be treated as reviewer-cleared for continued forward work: the current bridge/canonicalization contracts are stable enough to keep advancing the planned value-SSA line without pausing for more result-layer-only review unless a new concrete bug appears.
- Lower-IR now also has a first dedicated CFG/def-site analysis slice for the real conversion mainline: predecessor counts, reachability, dominators, immediate dominators, dominator-tree children, dominance frontiers, temp definition-block collection, and iterated phi placement can now be computed directly from verifier-legal lower-IR functions.
- Lower-IR analysis authority now also includes a representative multi-backedge loop-header join-temp family: one carried temp may reach the loop header from the entry seed plus multiple distinct backedge blocks, and predecessor/dom/frontier/phi-placement facts are now regression-locked on that shape.
- Lower-IR analysis authority also now includes a representative multi-carried-temp loop-header family: one loop header may require parallel phi candidates for multiple independent temps at once, and the batch temp-to-phi-candidate surface is expected to expose both columns correctly.
- Lower-IR analysis authority now also includes a representative nested-loop carried-temp family: an outer carried temp may legitimately have no phi candidate at an inner loop header, while an inner-loop-carried temp may still conservatively propagate phi-candidate marking outward through iterated dominance-frontier closure to the outer header.
- Lower-IR analysis authority also now includes a representative "same temp carried by both outer and inner loop" family: the current phi-candidate surface must expose both loop headers for that temp, matching the bridge's ability to materialize nested phis for one carried temp across two loop levels.
- Lower-IR analysis authority also now includes a stronger nested same-temp family where the inner loop itself has multiple backedges: the current phi-candidate surface still exposes both the outer and inner headers for that temp, matching the bridge's nested-phi behavior without needing extra bridge-only heuristics.
- Lower-IR analysis now also owns the batch temp-to-phi-candidate precompute surface: `lower_ir_compute_temp_phi_candidates(...)` is the input-layer authority for per-temp phi-candidate matrices, and `value_ssa` conversion should prefer consuming that API over rebuilding the same matrix in SSA-local helpers.
- Lower-IR analysis now also owns the compact predecessor-list surface in addition to the predecessor matrix/counts. Conversion and phi-finalization should prefer consuming that input-layer predecessor list rather than rescanning the full matrix in SSA-local loops.
- Lower-IR analysis now also owns the compact successor-list surface in addition to raw terminator edges. Future conversion cleanup should prefer consuming that input-layer successor list rather than re-decoding branch/jump structure ad hoc inside SSA-local orchestration.
- Lower-IR analysis now also owns a block-local phi-temp view in addition to the flat temp-to-block candidate matrix. Conversion should prefer consuming per-block phi-temp lists when iterating phi work, rather than rescanning the whole temp space for every join/header block.
- Current value-SSA conversion no longer keeps the flat `phi_candidate_blocks` matrix alive through the dominator walk. That matrix is now only a lower-IR-side intermediate used to build block-local phi-temp lists, after which conversion carries only the compact per-block view.
- Current value-SSA conversion now also stores pending-phi creation state with one fewer parallel array: `pending_phi_ids == SIZE_MAX` is the sole "phi not created yet" sentinel, so the old `pending_phi_present` bitmatrix is gone. This does not change bridge authority, but it continues to shrink the predecessor-state machinery that remains during the transition to a rename-driven construction.
- Value-SSA conversion has now started consuming that lower-IR analysis surface directly: the current `lower_ir -> value_ssa` path still preserves the same representative output shapes, but predecessor facts and phi-candidate placement are no longer sourced only from SSA-side prototypes.
- Value-SSA conversion now also consumes the lower-IR dominator-tree traversal surface directly while processing blocks. The current bridge still preserves the same representative output shapes, but conversion no longer relies on an internal topo-order-with-block-index fallback once lower-IR CFG analysis is available.
- Current value-SSA conversion coverage now also includes a representative multi-backedge loop-carried temp family: a loop header may merge the entry seed plus multiple distinct backedge definitions into one phi as long as the carried state is still representable through the current lower-IR temp/value contract.
- Current value-SSA conversion coverage also includes a representative multi-carried-temp loop family: one loop header may simultaneously carry multiple independent temps and lower them into parallel header phis without leaving the current raw bridge authority family.
- Current value-SSA conversion now also tolerates nested-loop inheritance without inventing spurious inner-header phis: when lower-IR input analysis says a temp has no phi candidate at an inner loop header, but already-processed predecessors agree on the carried value, conversion may tentatively inherit that value instead of failing early. Representative nested-loop carried-temp coverage is now part of bridge authority.
- Current value-SSA conversion must also stay insensitive to incidental sibling block-id order inside the supported CFG families. If a join/header is visited before some predecessors because dominator-tree child traversal sees it earlier, conversion may only pre-create a pending phi when all already-processed predecessors are still absent, not when any already-processed predecessor has already proven the temp invalid on that edge. A scrambled-diamond regression now locks this boundary.
- Current value-SSA entry-map construction now defaults to immediate-dominator inheritance for non-phi temps. Predecessor-state merging is still part of the bridge, but it is now narrowed to actual phi-candidate temps instead of recomputing every temp's block entry value from predecessor readiness.
- Current value-SSA conversion no longer carries the old `out_valid/current_valid` bitsets. Temp availability is now represented by the absence of a current binding in the rename state plus `SIZE_MAX` sentinels inside the remaining predecessor-facing snapshot surfaces.
- Lower-IR input analysis now also has a direct `lower_ir_compute_temp_phi_candidate_lists(...)` helper. Consumers that only need "which temps are phi candidates in this block" should prefer that block-local API instead of materializing and carrying a full temp-by-block phi-candidate bitmatrix themselves.
- Lower-IR input analysis now also has a direct `lower_ir_compute_block_successor_phi_use_lists(...)` helper. Consumers that only need "which temp values must this predecessor block snapshot because successor phis may read them later" should prefer that block-local successor-demand API instead of retaining a full temp-space exit map.
- Lower-IR input analysis now also has a pruned phi-candidate surface: `lower_ir_collect_temp_live_in_blocks(...)` can compute per-temp live-in blocks, and `lower_ir_compute_pruned_temp_phi_candidate_lists(...)` intersects live-in with iterated phi placement so downstream SSA construction can consume a tighter block-local phi list than the older conservative candidate matrix.
- That stricter input surface is precise for long-lived conversion state, but the current setup path still uses a temporary `raw_phi_candidate_temps` scratch buffer before compacting into the final block-local pruned list. So "bridge state is compact" is already true; "the whole setup path never materializes a raw temp-space scratch" is not yet true.
- Current value-SSA conversion now follows the strict dominator-tree construction path: it precreates all pruned candidate phis before traversal, enters each block by binding those phi results into the current rename scope, rewrites block-local lower-ir temp uses/defs only through the dominator-scope rename state, and fills successor phi incoming edges directly on predecessor leave.
- Current value-SSA conversion no longer relies on predecessor-readiness snapshots, deferred successor-phi value surfaces, or patch-up backfill to build the normal SSA graph. The remaining finalize step is just a completeness check that rejects any precreated phi still missing incoming edges after dominator-tree construction.
- Current value-SSA conversion still runs a verifier-safe alpha-renaming pass after strict construction so raw bridge dumps remain stable and dense in dominator order even though phis are precreated before DFS.
- Deferred complexity cleanups in earlier layers should stay deferred unless they block the current value-SSA path directly; do not reopen front-end or scope-internal data-structure work speculatively while the next active milestone is the first small value-SSA conversion slice.
- Lower-IR verifier now also has real temp-def/use guards instead of only temp-range checks: each temp must have at least one defining instruction, same-block redefinitions are rejected, and every temp use must be available on all incoming paths. This still intentionally permits the current lowered join-temp pattern where the same temp is materialized in mutually exclusive predecessor blocks and then returned in a join block.
- Lower-IR temp-definition checks only treat instructions with `has_result=1` as actual temp definers; non-value-effect instructions like `store_local` / `store_global` cannot smuggle a fake temp definition through an ignored `result` payload.
- Lower-IR verifier also now has a minimal declaration/signature shape contract: `parameter_count` must match the parameter-prefix locals, and ordinary declaration-only functions must not hide extra non-parameter locals or temp state. Reserved runtime-init helper names stay routed through their dedicated helper-shape rules instead of these generic declaration checks.
- Lower-IR source-lowered regressions now also lock exact runtime-init dump shapes: `__global.init` writes runtime-initialized globals through `store_global`, `main` startup flow reloads globals through explicit `load_global`, and the no-`main` fallback preserves `__program.init` as the single-call wrapper around `__global.init`.
- Lower-IR exact-dump coverage now includes representative control-flow-heavy slot cases too: single-arm and two-arm `if` joins lower local writes through `store_local` plus join-block `load_local`, short-circuit branch conditions materialize per-block `load_local` values, and `while` backedges preserve canonical temps while inserting extra slot-materialization temps above the canonical temp range.
- Lower-IR exact-dump coverage now also reaches loop-step and multi-branch writeback shapes: `while`-break exits, `for` init/step CFGs, nested loop break/continue control, and branch-local `store_global` followed by join-block `load_global` are all regression-locked as current authority.
- Lower-IR exact-dump coverage also now includes call-heavy CFG slices: declared-call `for` conditions/steps (including short-circuit `&&` conditions) lower call args through explicit `load_local`, and branchy `main` functions with runtime-global startup still keep `__global.init` as the first entry instruction while later user-expression temps retain canonical numbering precedence.
- Lower-IR exact-dump coverage now also includes representative value-materialization shapes: local `&&` values, ternary value expressions, declared-call logical values, and nested logical-mix value materialization. At this point the current Phase 2 authority covers straight-line lowering, control-flow slot materialization, call-heavy CFGs, runtime-init/startup interaction, and value-materialization joins; further expansion should be need-driven rather than open-ended.
- CFG cleanup is currently limited to safe local rewrites: constant/same-target branch folding, empty-jump threading, `jmp -> ret`, and single-predecessor jump-block merge.
- Shared internal pass facts now include temp analysis (use counts, mov-copy facts, recursive temp constants), CFG analysis (reachable blocks, predecessor counts), and instruction side-effect classification.
- Safe fold extensions currently include immediate/immediate folding, single-immediate identity reductions, and same-operand simplifications; avoid folds that erase UB checks such as division/modulo by a possibly zero operand or invalid shift validation.
- Treat `call` as effectful even when its result temp is dead; direct DCE and the default pipeline must preserve those calls while removing surrounding pure work.
- Temp copy/constant facts are only sound for uniquely defined temps; verifier-legal cross-block join-temp materialization means shared temp analysis must invalidate those facts once a temp is defined on multiple mutually exclusive paths.
- Shared temp analysis now has explicit def/use-oriented facts too: definition counts and unique def-sites are first-class analysis outputs, and copy/constant propagation should key off those facts instead of hidden overwrite-prone state.
- Shared temp analysis now also records explicit unique use-site facts (including operand kind and site location), and temp-copy propagation is allowed to consume those facts for direct single-use rewrites before falling back to broader scans.

- Shared `IrValueRef` equality is now a reusable pass utility; CFG cleanup may use it for local value-preserving rewrites such as folding a branch whose two instruction-free successors both `ret` the same non-temp value.
- Same-return diamond folding is sound for equal temp returns too when both successor blocks are instruction-free: verifier-valid empty successors that `ret tmp.N` imply the temp is already available at the branch block exit, so the branch can collapse to `ret tmp.N` directly.
- CFG same-return diamond folding should compare returns by shared temp-analysis equivalence, not only by literal value-ref identity: safe constant resolution and safe copy resolution can expose equal returns even when the two empty successors use different temps.
- The same-return CFG rule now also covers successor blocks of the form `tmp = mov value; ret tmp` when both moved values resolve to the same branch-exit-available value; this stays conservative because the only erased instruction is the pure `mov`, and the folded `ret` uses the resolved source value rather than a successor-local temp.
- Dead-temp elimination must not treat every binary instruction as silently pure: dead-result `div`/`mod`/shift ops remain non-erasable because they still embody runtime validation / failure points even when their result temp is unused.
- Binary identity folding must not erase shift-by-zero operations under the current IR safety policy, because shift validity still depends on operand constraints beyond the immediate zero count.
- The default pass pipeline may need a post-DCE CFG revisit: temp propagation can canonicalize successor returns, DCE can then empty those successor blocks, and a final `simplify-cfg` + dead-temp cleanup pass can safely collapse the newly exposed branch diamond.
- Pipeline-level multi-def join-temp regressions should lock semantic path distinction, not a specific pre-cleanup join-block shape: full-pipeline CFG cleanup may legitimately sink the join `ret tmp.N` into the two successor return blocks as long as the branch and the distinct per-path return values remain preserved.
- Pipeline-level side-effect regressions should include CFG cleanup interaction, not only straight-line DCE: a dead-result `call` inside a branch that later becomes constant-folded must still survive after branch simplification, dead-block cleanup, and the post-DCE CFG revisit.
- For a few now-stable representative pipeline shapes, exact dump form is part of the regression contract, not just semantic equivalence: current examples include branch-local dead-result call cleanup, multi-def constant-path preservation, post-DCE same-return collapse to a single `ret` block, same-temp branch fold collapse to `ret 7`, and single-predecessor nonempty merge collapse to one surviving instruction plus `ret`.
- Representative pipeline regressions should also exercise the default pass order step-by-step, not only as one opaque pipeline call: verifier success after each individual pass and preservation of key invariants through the sequence are now part of the safety story.
- The public pass-pipeline contract now includes verifier gating at the API boundary: `ir_pass_run_pipeline(...)` rejects verifier-invalid input IR up front and rejects verifier-invalid pass output immediately after the offending pass.
